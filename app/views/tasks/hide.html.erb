<h1>非表示タスク一覧</h1>
<ul>
  <% @tasks.each do |task| %>
    <%# タスク名からタスク詳細画面へのリンクで遷移できること。 %>
    <li><%= link_to task.title, task %> <%= task.created_at.strftime('%Y年%m月%d日') %></li>
  <% end %>
</ul>
<%# タスク非表示画面からタスク一覧画面へのリンクで遷移できること。%>
<%= link_to '一覧', tasks_path %>